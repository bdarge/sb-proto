syntax = "proto3";

package lang;

import "pb/model.proto";

service LangService {
  rpc getLang(LangGetRequest) returns (LangGetResponse) {}
}

message LangGetRequest {
}

message LangGetResponse {
  uint32 status = 1;
  string error = 2;
  repeated model.LangData data = 3;
}
